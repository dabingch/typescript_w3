var carYear = 2001;
var carType = "Toyota";
var carModel = "Corolla";
// can not define same variable in different files?
var carAlias = {
    year: carYear,
    type: carType,
    model: carModel
};
console.log(carAlias);
